version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: hotel-app:latest
    container_name: my-hotel-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
